# add-red-model-sharing

Add SHA1-based URL sharing for grammar code, string, and action code with DBIish+SQLite snapshot persistence

> **Note on naming**: This change was originally scoped to use Red ORM, but the implementation uses DBIish+SQLite. The `add-red-model-sharing` name is historical — see [design.md](./design.md) for the decision analysis.
